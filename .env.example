# ===================================================================
# BLACKWALL — Environment Variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# NEVER commit the .env file to version control.
# ===================================================================

# ---------- Database ----------
RECON_DB_USER=recon
RECON_DB_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD
RECON_DB_HOST=127.0.0.1
RECON_DB_PORT=5432
RECON_DB_NAME=recon

# ---------- Server ----------
# RECON_HOST=127.0.0.1
# RECON_PORT=8000

# ---------- Storage ----------
# RECON_ARTIFACT_ROOT=data/artifacts
# RECON_REPORT_ROOT=data/reports

# ---------- Jobs ----------
# RECON_DEFAULT_CONCURRENCY=2
# RECON_MAX_CONCURRENCY=4
# RECON_DEFAULT_CONTAINER_CPU=2
# RECON_DEFAULT_CONTAINER_MEMORY_GB=2
